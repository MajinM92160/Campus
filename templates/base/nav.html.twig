<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
<link rel="stylesheet" href="{{ asset('css/styleGreg.css') }}" type="text/css">
<script src="https://kit.fontawesome.com/db2e6dd81c.js" crossorigin="anonymous"></script>


    {% if app.user.roles == ["student"] %}
    {# ============ CLASSIQUE =============== #}
    <nav class="menuBase">
        <div class="conteneurlogoNav">
            <a href="{{ path('listEvent') }}">
                <img class="logoNav" src="{{ asset('img/logo1.png') }}" alt="logo du site campus">
            </a>
        </div>
        <div class="lienNav">
            <a href="{{ path('listEvent') }}">Événements</a>
            <a href="{{ path('userTickets') }}">Mes tickets</a>
            <a href="{{ path('studentInfos') }}">Mes informations</a>
            <a href="{{ path('panier') }}">Panier</a>
            <a href="{{ path('logout') }}">Déconnexion</a>
        </div>
    </nav>
    {# ============ BURGER =============== #}
    <nav class="menuBurger" role="navigation" style="display:none;">
        <div id="menuToggle">
          <!--
          A fake / hidden checkbox is used as click reciever,
          so you can use the :checked selector on it.
          -->
          <input type="checkbox" />
          
          <!--
          Some spans to act as a hamburger.
          
          They are acting like a real hamburger,
          not that McDonalds stuff.
          -->
          <span></span>
          <span></span>
          <span></span>
          
          <!--
          Too bad the menu has to be inside of the button
          but hey, it's pure CSS magic.
          -->
          <ul id="menu">
            <a href="{{ path('listEvent') }}"><li>
                <img class="logoNav" src="{{ asset('img/logo1.png') }}" alt="logo du site campus">
            </li></a>
            <a href="{{ path('userTickets') }}"><li>Mes tickets</li></a>
            <a href="{{ path('studentInfos') }}"><li>Mes informations</li></a>
            <a href="{{ path('panier') }}"><li>Panier</li></a>
            <a href="{{ path('logout') }}"><li>Déconnexion</li></a>
          </ul>
        </div>
    </nav>
    {% endif %}


    
    {% if app.user.roles == ["office"] %}
    {# ============ CLASSIQUE =============== #}
    <nav class="menuBase">
        <div class="logoNav">
            <a href="{{ path('listEvent') }}">
                <img class="logoNav" src="{{ asset('img/logo1.png') }}" alt="logo du site campus">
            </a>
        </div>
        <div class="lienNav">
            <a href="{{ path('listEvent') }}">Mes événements</a>
            <a href="{{ path('officeListEvent') }}">Créer un événement</a>
            <a href="{{ path('listStudents') }}">Mes étudiants</a>
            <a href="{{ path('officeInfos') }}">Mes informations</a>
            <a href="{{ path('logout') }}">Déconnexion</a>
        </div>
    </nav>
    {# ============ BURGER =============== #}
    <nav class="menuBurger" role="navigation" style="display:none;">
        <div id="menuToggle">
          <!--
          A fake / hidden checkbox is used as click reciever,
          so you can use the :checked selector on it.
          -->
          <input type="checkbox" />
          
          <!--
          Some spans to act as a hamburger.
          
          They are acting like a real hamburger,
          not that McDonalds stuff.
          -->
          <span></span>
          <span></span>
          <span></span>
          
          <!--
          Too bad the menu has to be inside of the button
          but hey, it's pure CSS magic.
          -->
          <ul id="menu">
            <a href="{{ path('listEvent') }}">Mes événements</a>
            <a href="{{ path('officeListEvent') }}">Créer un événement</a>
            <a href="{{ path('listStudents') }}">Mes étudiants</a>
            <a href="{{ path('officeInfos') }}">Mes informations</a>
            <a href="{{ path('logout') }}">Déconnexion</a>
          </ul>
        </div>
    </nav>
    {% endif %}

